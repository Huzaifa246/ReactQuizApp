const Footer = () => {
  return (
    <div
      style={{
        textAlign: "center",
        marginBottom: 10,
      }}
    >
      <p style={{ cursor: "pointer" }}>Made with ✨♥✨ by <em> {" HUZAIFA "} </em> </p>
    </div>
  );
};

export default Footer;
